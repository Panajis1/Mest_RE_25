# Compatibility shim.
# Primary dependency source: pyproject.toml
# Reproducible fully-pinned environment: requirements.lock
#
# Typical installs:
#   pip install -e ".[dev]"
#   pip install -r requirements.lock
#
# This file intentionally delegates to pyproject dependencies to avoid drift.
-e .[dev]