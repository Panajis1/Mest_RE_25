# RE production overrides — inherits all defaults from default.yaml
# Update data paths to match your local data layout before running.

data:
  re_data_dir: "C:/Users/jiniy/Desktop/CS/ETHZ_ALL"
  metadata_dir: "C:/Users/jiniy/Desktop/CS/ETHZ_ALL"

pipeline:
  n_workers: "auto"
  batch_size: 500
  autosave_every_n_customers: 300

detectors:
  enabled:
    - pv
    - battery
    - ac
    - heat_pump
    - ev

models:
  pv:
    capacity_bootstrap_n: 10
  ac:
    prob_threshold: 0.72